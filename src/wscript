
def build(bld):
    bld.shlib(target = 'ndarray', 
                source = 'ndarray.cpp')
    
    bld.shlib(target = 'ndarray_test', 
                source = 'ndarray_test.cpp', 
                use = 'tclap ndarray')
